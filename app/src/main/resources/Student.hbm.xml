<?xml version = "1.0" encoding = "utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC
        "-//Hibernate/Hibernate Mapping DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping>
    <class name = "by.dutov.jee.people.Student" table = "student">

        <id name = "id" type = "int" column = "id">
            <generator class="identity"/>
        </id>

        <property name = "userName" column = "user_name"/>
        <property name = "password" column = "password"/>
        <property name = "salt" column = "salt"/>
        <property name = "name" column = "name"/>
        <property name = "age" column = "age"/>

        <set name="grades" table="grades" inverse="true">
            <key column="student_id" not-null="false" />
            <one-to-many class="by.dutov.jee.people.Grades"/>
        </set>

        <set name="groups" table="group_student">
            <key column="student_id" />
            <many-to-many column="group_id" class="by.dutov.jee.group.Group"/>
        </set>

    </class>
</hibernate-mapping>